name: metroom
description: A new Flutter project.

publish_to: "none" # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.17.3 <3.0.0"

dependencies:
  carousel_slider: ^4.1.1
  cloudinary_sdk: ^4.0.2+10
  concentric_transition: ^1.0.3
  cupertino_icons: ^1.0.2
  file_picker: ^4.6.1
  flutter:
    sdk: flutter
  flutter_rating_bar: ^4.0.1
  flutter_svg: ^1.1.1
  google_fonts: ^3.0.1
  intl: ^0.17.0
  provider: ^6.0.3
  salomon_bottom_bar: ^3.3.1
  shared_preferences: ^2.0.15
  supabase: ^0.3.4+1
  url_launcher: ^6.1.4

dev_dependencies:
  flutter_launcher_icons: "^0.9.2"
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

flutter_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/logo/logo.png"

flutter:
  uses-material-design: true
  assets:
    - assets/svg/
    - assets/logo/

# fonts:
#   - family: UniNeue
#     fonts:
#       - asset: assets/fonts/UniNeue-HeavyItalic.otf
#       - asset: assets/fonts/UniNeue-Light.otf
